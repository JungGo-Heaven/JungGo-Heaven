package com.example.junggoheaven.domain.image.repository;

import com.example.junggoheaven.domain.image.entity.ChatRoomImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChatroomImageRepository extends JpaRepository<ChatRoomImage, Long> {
}
